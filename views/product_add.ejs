<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เพิ่มเมนูอาหาร</title>
    
    <!-- นำเข้า Bootstrap CSS เพื่อจัดการสไตล์ -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    
    <style>
        /* style.css */
        body {
            /* ตั้งค่าสีพื้นหลังและรูปพื้นหลัง */
            background: url('https://images.unsplash.com/photo-1605926637512-c8b131444a4b?q=80&w=1780&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D') no-repeat center center/cover;
            padding-top: 50px;
    height: 100vh;
    width: 100vw;
    background-position: center;
    background-size: cover;
        }

        .container {
            background-color: #ffffff; /* สีพื้นหลังของฟอร์ม */
            padding: 30px; /* ระยะห่างภายใน */
            border-radius: 10px; /* ขอบมน */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* เงา */
            max-width: 600px; /* กำหนดความกว้างสูงสุด */
            margin: auto; /* จัดฟอร์มให้อยู่ตรงกลาง */
        }

        h2 {
            color: #333; /* สีหัวข้อ */
            text-align: center; /* จัดหัวข้อให้อยู่ตรงกลาง */
            margin-bottom: 20px; /* ระยะห่างด้านล่าง */
        }

        .form-label {
            font-weight: bold; /* ทำให้ชื่อฟิลด์หนา */
        }

        .form-control {
            border-radius: 5px; /* ขอบมน */
            border: 1px solid #ced4da; /* สีขอบ */
        }

        .form-control:focus {
            border-color: #007bff; /* เปลี่ยนสีขอบเมื่อโฟกัส */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* เงาเมื่อโฟกัส */
        }

        .btn-primary {
            background-color: #007bff; /* สีพื้นหลังของปุ่ม */
            border: none; /* ไม่มีขอบ */
            border-radius: 5px; /* ขอบมน */
            padding: 10px 20px; /* ระยะห่างด้านใน */
            font-size: 16px; /* ขนาดฟอนต์ */
        }

        .btn-primary:hover {
            background-color: #0056b3; /* สีเมื่อเอาเมาส์ไปอยู่เหนือปุ่ม */
            transition: background-color 0.3s ease; /* การเปลี่ยนสีอย่างนุ่มนวล */
        }

        .mb-3 {
            margin-bottom: 1.5rem; /* ระยะห่างด้านล่างของแต่ละฟิลด์ */
        }
    </style>
</head>
<body>
    <!-- แทรก headerbar จากไฟล์อื่น -->
    <%- include('headerbar') %>
    
    <div class="container my-4">
        <h2>เพิ่มเมนูอาหารใหม่</h2>
        
        <!-- ฟอร์มสำหรับเพิ่มเมนูอาหาร -->
        <form action="/add-product" method="POST" enctype="multipart/form-data">
            <!-- ฟิลด์กรอกชื่ออาหาร -->
            <div class="mb-3">
                <label for="name" class="form-label">ชื่ออาหาร</label>
                <input type="text" class="form-control" id="name" name="name" required>
            </div>
            
            <!-- ฟิลด์เลือกประเภทอาหาร -->
            <div class="mb-3">
                <label for="category" class="form-label">ประเภทอาหาร</label>
                <select name="category" id="category" required>
                    <% products_type.forEach(pt => { %>
                        <!-- แสดงประเภทอาหารที่ดึงมาจากเซิร์ฟเวอร์ -->
                        <option value="<%= pt.pt_id %>"><%= pt.pt_name_th %></option>
                    <% }); %>
                </select>
            </div>
            
            <!-- ฟิลด์กรอกราคาอาหาร -->
            <div class="mb-3">
                <label for="price" class="form-label">ราคา</label>
                <input type="number" class="form-control" id="price" name="price" required>
            </div>
            
            <!-- ฟิลด์เลือกรูปภาพของอาหาร -->
            <div class="mb-3">
                <label for="img" class="form-label">รูปภาพ</label>
                <input type="file" class="form-control" id="img" name="img" required>
            </div>
            
            <!-- ปุ่มส่งข้อมูลเพื่อลงทะเบียนเมนูใหม่ -->
            <button type="submit" class="btn btn-primary">เพิ่มเมนู</button>
        </form>
    </div>
</body>
</html>
