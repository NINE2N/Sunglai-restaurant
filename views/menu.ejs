<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
    <!-- นำเข้า Bootstrap เพื่อใช้ตกแต่งหน้าเว็บ -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* ตั้งค่าพื้นหลัง */
        body {
            /* ใช้ภาพเป็นพื้นหลังและปรับขนาดให้ครอบคลุมจอ */
            background: url('https://images.unsplash.com/photo-1605926637512-c8b131444a4b?q=80&w=1780&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D') no-repeat center center/cover;
            color: #333;
            padding-top: 50px; /* เว้นระยะด้านบนของเนื้อหา */
        }
        /* การตกแต่งการ์ด */
        .card {
            /* โค้งมนและตั้งค่าสีพื้นหลัง */
            border-radius: 10px;
            background-color: #333;
            color: aliceblue;
        }
        /* กำหนดรูปภาพในการ์ด */
        .card-img-top {
            width: 100%;
            height: 200px; /* กำหนดความสูงของรูป */
            object-fit: cover; /* ปรับรูปให้ไม่ผิดสัดส่วน */
            /* ทำให้ขอบบนของรูปภาพโค้งมน */
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
        }
        /* กำหนดสีฟอนต์สำหรับหัวข้อ */
        h1, h2 {
            color: #fff;
        }
    </style>
</head>
<body>
    <!-- แทรกแถบเมนู -->
    <%- include('headerbar') %>

    <!-- คอนเทนเนอร์หลักของเมนู -->
    <div class="container my-4">
        <h1 class="text-center mb-4">Menu</h1>

        <!-- แสดงหมวดหมู่สินค้าจากข้อมูลที่ส่งมาจากเซิร์ฟเวอร์ -->
        <% Object.keys(groupedProducts).forEach(category => { %>
            <!-- แสดงชื่อหมวดหมู่ (ภาษาไทยและภาษาอังกฤษ) -->
            <h2 class="text-center mt-4"><%= groupedProducts[category].categoryNameTh %> (<%= groupedProducts[category].categoryName %>)</h2>
            <div class="row">
                <!-- แสดงสินค้าทั้งหมดในแต่ละหมวดหมู่ -->
                <% groupedProducts[category].products.forEach(product => { %>
                    <div class="col-md-4 mb-4">
                        <!-- แสดงการ์ดสินค้าแต่ละรายการ -->
                        <div class="card">
                            <!-- รูปภาพสินค้า -->
                            <img src="/uploads/<%= product.img %>" class="card-img-top" alt="<%= product.name %>">
                            <div class="card-body">
                                <!-- ชื่อและราคาสินค้า -->
                                <h5 class="card-title"><%= product.name %></h5>
                                <p class="card-text">Price: $<%= product.price %></p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% }); %>
        
        <!-- นำเข้า Bootstrap Bundle JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
